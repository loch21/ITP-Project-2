// Correct import (assuming CSS file is Nav.css in same directory)
import './Nav.css';

function Nav() {
  return (
    <nav className="nav">
      {/* Your navigation links */}
    </nav>
  );
}

export default Nav;